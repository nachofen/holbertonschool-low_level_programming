#include "main.h"
/**
 * _abs - This is a description
 * @c: input char
 * Return: 1 if is lowercase or 0 if is uppercase
 */
int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
		return (c * -1);
	return (0);
}
