#include "main.h"
#include <stdio.h>
/**
 * _isupper - A function that checks for uppercase char
 * @c: An input char
 * Return: 1 if c is uppercase or 0 ifnot
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0)
}
