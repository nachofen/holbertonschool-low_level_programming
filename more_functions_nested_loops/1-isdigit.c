#include "main.h"
#include <stdio.h>
/**
 * _isdigit - A function that checks for uppercase char
 * @c: An input char
 * Return: 1 if c is uppercase or 0 ifnot
 */
int _isdigit(int c)
{
	if (c >=  48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
