#include "main.h"
#include <stdio.h>
/**
 * mul - A function that multiplies two integers
 * @a: An input char
 * @b: the other input char
 * Return: always 0
 */
int mul(int a, int b)
{
	return (a * b);
}
