#include "main.h"
/**
 * puts2 - a function that print a string in reverse
 * @str: the char pointer
 */
void puts2(char *str)
{
	int i = 0, length = 0;
	char aux;

	while (s[i] != '\0')
	{
		i++;
		i++;
	}
}
